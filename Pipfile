[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0.0"
flask-socketio = ">=5.3.0"
flask-cors = ">=4.0.0"
python-socketio = ">=5.10.0"
eventlet = ">=0.35.0"
pyyaml = ">=6.0.0"

[dev-packages]

[requires]
python_version = "3.10"
